<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kr.co.sist.dao.mainData">
	<select id="mainCarView" resultType="kr.co.sist.domain.CarData">
		SELECT MODEL, CAR_YEAR, CAR_IMG
		FROM	(SELECT MODEL, CAR_YEAR, CAR_IMG
				FROM CAR_MODEL
				ORDER BY HIREDATE DESC)
		WHERE ROWNUM BETWEEN 1 AND 6		
	</select>
	<select id="empView" resultType="kr.co.sist.domain.EmpData">
		
		SELECT ENAME, EMPNO, JOB
		FROM (SELECT ENAME, EMPNO, JOB
				FROM EMP
				ORDER BY SAL DESC)
		WHERE ROWNUM BETWEEN 1 AND 5
	</select>
	
	
</mapper>
